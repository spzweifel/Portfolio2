import React from 'react';
// provides access to DOM-specific methods
import ReactDOM from 'react-dom';
import App from './App';

// renders React components to the DOM.
ReactDOM.render(<App />, document.getElementById('root'));